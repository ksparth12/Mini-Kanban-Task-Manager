{
  "name": "mini-kanban-task-manager",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:all": "bash ./scripts/dev.sh",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev -- --host ${FRONTEND_HOST:-127.0.0.1} --port ${FRONTEND_PORT:-4173}",
    "test": "echo 'No root tests configured.'"
  }
}
